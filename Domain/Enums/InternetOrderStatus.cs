namespace MiNegocioCR.Api.Domain.Enums;

/// <summary>Valores en <c>InternetOrder.Status</c> (int). No reordenar.</summary>
public enum InternetOrderStatus
{
    Created = 0,
    Purchased = 1,
    Received = 2,
    Delivered = 3,
    Cancelled = 4
}
